server:
  port: 8085
spring:
  application:
    name: rate-limit-service
  data:
    redis:
      host: localhost
      port: 6379


management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

spring:
  cache:
    type: redis

